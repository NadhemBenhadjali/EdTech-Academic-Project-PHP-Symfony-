<form method="post" action="{{ path('admin_chapter_delete', {'id': chapter.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chapter.id) }}">
    <button class="btn-action btn-danger">Supprimer</button>
</form>
