<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}EduTech Academy{% endblock %}</title>

    <!-- Favicon -->
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>🚀</text></svg>">

    <!-- Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Plus+Jakarta+Sans:wght@700;800&display=swap" rel="stylesheet">

    {% block stylesheets %}
        <style>
            /* === Professional Redesign by Copilot === */
            :root {
                --bg-dark: #111827;
                --bg-light: #f9fafb;
                --border-color: #374151;
                --text-primary-dark: #f9fafb;
                --text-secondary-dark: #9ca3af;
                --text-primary-light: #1f2937;
                --accent: #4f46e5;
                --accent-hover: #4338ca;
                --shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.05);
                --shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1);

                /* Details / glass theme variables (centralisées) */
                --details-bg-color: #111827;
                --details-gradient-start: #4f46e5;
                --details-gradient-end: #ec4899;
                --glass-bg: linear-gradient(145deg, rgba(255, 255, 255, 0.05) 0%, rgba(255, 255, 255, 0.01) 100%);
                --glass-border: rgba(255, 255, 255, 0.15);
                --text-light: #e5e7eb;
                --text-muted: #9ca3af;
                --accent-gradient: linear-gradient(90deg, var(--details-gradient-start) 0%, var(--details-gradient-end) 100%);
                --shadow-color: rgba(0, 0, 0, 0.5);
                --transition-curve: cubic-bezier(0.25, 0.8, 0.25, 1);
                /* Application background (uniform across pages) */
                /* couleur fournie par l'utilisateur (opaque) */
                --app-bg: #0F0C29;
            }

            *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

            html {
                scroll-behavior: smooth;
            }

            body {
                font-family: 'Inter', sans-serif;
                /* fond uniforme demandé */
                background: var(--app-bg);
                color: var(--text-primary-light);
                display: flex;
                flex-direction: column;
                min-height: 100vh;
            }

            a { text-decoration: none; color: inherit; }

            /* --- Header --- */
            header {
                display: flex;
                justify-content: space-between;
                align-items: center;
                padding: 1rem 2.5rem;
                background: var(--bg-dark);
                color: var(--text-primary-dark);
                border-bottom: 1px solid var(--border-color);
                box-shadow: var(--shadow-md);
                position: sticky;
                top: 0;
                z-index: 1000;
            }

            header .logo {
                font-family: 'Plus Jakarta Sans', sans-serif;
                font-size: 1.5rem;
                font-weight: 800;
            }

            header nav {
                display: flex;
                align-items: center;
                gap: 1.5rem;
            }

            header nav .nav-link {
                font-weight: 500;
                color: var(--text-secondary-dark);
                transition: color 0.2s ease;
                position: relative;
                padding: 0.5rem 0;
            }
            header nav .nav-link:hover { color: var(--text-primary-dark); }

            header nav .nav-link::after {
                content: '';
                position: absolute;
                bottom: 0;
                left: 0;
                width: 100%;
                height: 2px;
                background: var(--accent);
                transform: scaleX(0);
                transform-origin: right;
                transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            }
            header nav .nav-link:hover::after {
                transform: scaleX(1);
                transform-origin: left;
            }

            .user-menu {
                position: relative;
            }

            .user-avatar {
                width: 40px;
                height: 40px;
                border-radius: 50%;
                background: var(--accent);
                color: var(--text-primary-dark);
                display: grid;
                place-items: center;
                font-weight: 600;
                cursor: pointer;
                border: 2px solid var(--border-color);
                transition: transform 0.2s ease;
            }
            .user-avatar:hover {
                transform: scale(1.05);
            }

            .dropdown-menu {
                position: absolute;
                top: calc(100% + 10px);
                right: 0;
                background: var(--bg-dark);
                border: 1px solid var(--border-color);
                border-radius: 12px;
                padding: 0.5rem;
                box-shadow: var(--shadow-md);
                opacity: 0;
                visibility: hidden;
                transform: translateY(10px);
                transition: opacity 0.2s ease, transform 0.2s ease, visibility 0.2s;
                min-width: 180px;
            }

            .user-menu:hover .dropdown-menu {
                opacity: 1;
                visibility: visible;
                transform: translateY(0);
            }

            .dropdown-item {
                display: block;
                padding: 0.75rem 1rem;
                color: var(--text-secondary-dark);
                border-radius: 8px;
                transition: background-color 0.2s ease, color 0.2s ease;
            }
            .dropdown-item:hover {
                background-color: var(--accent);
                color: var(--text-primary-dark);
            }

            /* --- Main --- */
            main {
                flex-grow: 1;
            }

            /* --- Footer --- */
            footer {
                text-align: center;
                padding: 2rem 0;
                font-size: 0.9rem;
                color: var(--text-secondary-dark);
                background: var(--bg-dark);
                border-top: 1px solid var(--border-color);
            }

            /* --- Buttons (General Fallback) --- */
            .btn {
                display: inline-block;
                padding: 0.75rem 1.5rem;
                border-radius: 8px;
                background: var(--accent);
                color: var(--text-primary-dark);
                font-weight: 600;
                transition: all 0.2s ease;
                border: none;
            }
            .btn:hover {
                transform: translateY(-2px);
                background: var(--accent-hover);
                box-shadow: var(--shadow-md);
            }

            /* Back link — subtle text link without box, reused across templates */
            .btn-back {
                display: inline-flex;
                align-items: center;
                gap: 0.5rem;
                color: var(--text-secondary-dark);
                font-weight: 700;
                background: transparent;
                border: none;
                padding: 0;
                cursor: pointer;
                text-decoration: none;
                transition: color 160ms ease, transform 160ms ease;
                font-size: 0.98rem;
            }
            .btn-back:hover { color: var(--text-primary-dark); }
            .btn-back:focus-visible { outline: 3px solid rgba(79,70,229,0.18); outline-offset: 3px; border-radius: 6px; }
            .btn-back-icon { display:inline-block; font-size:16px; line-height:1; transform: translateX(0); transition: transform 160ms ease; }
            .btn-back:hover .btn-back-icon { transform: translateX(-3px); }

            /* Flash messages */
            .flash-container {
                max-width: 900px;
                margin: 1rem auto;
                padding: 0 1rem;
            }
            .flash {
                padding: 0.75rem 1rem;
                border-radius: 8px;
                margin-bottom: 0.75rem;
                font-weight: 600;
                box-shadow: 0 6px 18px rgba(0,0,0,0.06);
            }
            .flash-success { background: linear-gradient(90deg, #10b981 0%, #34d399 100%); color: #062f17; }
            .flash-error { background: linear-gradient(90deg, #ef4444 0%, #f97316 100%); color: #3a0a05; }
            .flash-info { background: linear-gradient(90deg, #60a5fa 0%, #93c5fd 100%); color: #0b2a4a; }

            /* --- Responsive --- */
            @media (max-width: 768px) {
                header {
                    padding: 1rem 1.5rem;
                }
                header .logo {
                    font-size: 1.25rem;
                }
                header nav {
                    gap: 1rem;
                }
            }

            /* Force uniform background and hide decorative gradients/orbs/backdrops */
            /* This ensures the entire app background is the single color defined in --app-bg */
            .background-container,
            .gradient-orb,
            .orb-1,
            .orb-2,
            .orb-3,
            .image-backdrop,
            .details-page-wrapper::before,
            .details-page-wrapper::after {
                display: none !important;
                background: none !important;
                box-shadow: none !important;
                opacity: 0 !important;
            }

            /* Ensure body uses the app background (redundant but explicit) */
            html, body, #main-content {
                background: var(--app-bg) !important;
            }
        </style>
    {% endblock %}
</head>
<body class="{% block body_class '' %}">

<!-- Header -->
<header>
    <a href="/" class="logo">EduTech</a>
    <nav>
        <a href="{{ path('student_courses') }}" class="nav-link">Mes Cours</a>
        {% if app.user %}
            <div class="user-menu">
                <div class="user-avatar">{{ (app.user.name|default(app.user.email))|slice(0,1)|upper }}</div>
                <div class="dropdown-menu">
                    <a href="{{ path('app_logout') }}" class="dropdown-item">Déconnexion</a>
                </div>
            </div>
        {% else %}
            <a href="{{ path('app_login') }}" class="nav-link">Connexion</a>
        {% endif %}
    </nav>
</header>

<!-- Main content -->
<main id="main-content">
    <div class="flash-container">
        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="flash flash-{{ label }}">{{ message }}</div>
            {% endfor %}
        {% endfor %}
    </div>
    {% block body %}{% endblock %}
</main>

<!-- Footer -->
<footer>
    &copy; {{ "now"|date("Y") }} EduTech Academy. Tous droits réservés.
</footer>

{% block javascripts %}
    {% block importmap %}{{ importmap('app') }}{% endblock %}
{% endblock %}
</body>
</html>
